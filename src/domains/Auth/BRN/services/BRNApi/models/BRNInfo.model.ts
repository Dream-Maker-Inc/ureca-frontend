import { TaxPlayerType } from "./TaxPlayer.type";
import { TaxPlayerState } from "./TaxPlayerState.type";

export type BrnInfo = {
  taxPlayerState: TaxPlayerState;
  taxPlayerType: TaxPlayerType;
};
